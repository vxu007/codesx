version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pdf-converter-web
    ports:
      - "3030:3030"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge